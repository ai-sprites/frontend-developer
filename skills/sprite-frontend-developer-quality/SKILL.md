---
name: sprite-frontend-developer-quality
description: "根据当前前端改动选择有价值的交互、浏览器、无障碍、性能和错误处理检查，并说明真实覆盖限制。"
---

# 前端质量检查

让检查回答当前改动是否破坏用户结果，使用项目现有工具，避免为小改动创建新的质量平台。本实践独立于角色和其他 Skill。

- 交互、浏览器、键盘及无障碍回归：读 [行为检查](references/behavior.md)。
- 性能、请求失败、错误信号和客户端风险：读 [性能与错误](references/performance-errors.md)。

先从差异找出可能受影响的行为，再选检查。执行后报告实际命令或操作、观察结果和未覆盖部分，不用静态格式检查冒充浏览器验证；需要保留证据时才落盘。

给人的回复和文档用简短白话，先说结果、影响和需要决定的事；细节与证据按需附上，不为每个角色强建文档。

确实需要用户决定时，用简短清单说明“要决定什么、为什么现在需要、可选做法及影响”，并在当前对话直接提问，不能只写进文档。已有答案和授权直接沿用；建议、未回复和“待确定”不算同意。只暂停依赖答案的部分，其余继续，普通实现细节自行处理。

需求迭代先核对已有依据和本次变化，说明哪些工作、角色和旧产物受影响；只做受影响的部分，无须重新召集全部角色。交接指出哪些旧结论仍适用、哪些已替代或需要核对，未验证的旧结论不能冒充当前结果。

需要留存或交接时，把结论与证据写入用户指定业务仓库/目录，或沿用项目位置；无约定时可用 `docs/artifacts/<feature-id>/frontend-verification.md`。交付所属仓库、根目录相对路径、真实链接与版本。可选索引用 `kind: "verification"` 和根目录相对 `path` 登记，附件单独列出，Markdown 链接相对文档；代码留原处，产物不放进本 Skill 目录。资料 host repo、版本和文件由用户指定或沿用项目明确约定，未提交或未推送如实说明；bridge 不替上游推送。

首次引入角色或实践时，整次接入只把未明确的资料 host repo 和产物 repo/目录合并询问一次，允许“暂无/稍后配置”并先完成接入；已指定角色、已答或已暂缓的项不重问，真正读取时再补必需版本、功能或文件。

当前任务确需跨 Git 读取、固定版本、比较或导出留存时，复用已有 bridge；缺失或能力不足时，AI 按 [artifact-bridge README 与接入手册](https://github.com/ai-sprites/artifact-bridge)把同一明确版本的完整 Skill 补齐到当前客户端已有目录，保留自定义并告知用户，无需再单独确认安装。用户拒绝安装或权限、网络不足时说明限制，继续可独立部分。普通本地文件和纯角色安装不触发补齐。
